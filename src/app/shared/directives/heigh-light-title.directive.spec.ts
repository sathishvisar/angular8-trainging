import { HeighLightTitleDirective } from './heigh-light-title.directive';

describe('HeighLightTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new HeighLightTitleDirective();
    expect(directive).toBeTruthy();
  });
});
