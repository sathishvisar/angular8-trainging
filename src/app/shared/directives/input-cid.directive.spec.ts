import { InputCidDirective } from './input-cid.directive';

describe('InputCidDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCidDirective();
    expect(directive).toBeTruthy();
  });
});
